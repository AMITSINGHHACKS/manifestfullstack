apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: devops
  template:
    metadata:
      labels:
        app: devops
    spec:
      containers:
      - name: devops
        image: truthaniket/devopsback:1.0.0-10
        ports:
        - containerPort: 5000

---

apiVersion: v1
kind: Service
metadata:
  name: service-html
spec:
  type: LoadBalancer
  ports:
  - port: 8082
    targetPort: 5000
  selector:
    app: devops
